[project]
name = "rs-movie"
version = "0.1.0"
description = "Movie search and detail viewer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "snowflake-connector-python",
    "python-dotenv",
]


